# Environment Variables Example

# Socket.IO Server URL
VITE_SOCKET_SERVER_URL=http://localhost:4000

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: AI Backend Integration (when implementing AI features)
# ⚠️ SECURITY WARNING: Never store AI provider API keys in VITE_ variables!
# VITE_ variables are embedded in the client bundle and exposed to users.
# Instead, keep API keys on your backend server and call your own API endpoint.
# Example: Create a backend route /api/ai that uses server-side env vars
# OPENAI_API_KEY=your-key-here (server-side only, no VITE_ prefix)
# ANTHROPIC_API_KEY=your-key-here (server-side only, no VITE_ prefix)

